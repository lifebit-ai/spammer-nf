{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "spammer-nf parameters",
  "type": "object",
  "properties": {
    "count": {
      "type": "integer",
      "default": 3000,
      "minimum": 1,
      "description": "Number of files to generate in the results folder."
    },
    "outdir": {
      "type": "string",
      "default": "results",
      "description": "Output directory to publish (collected by CloudOS)."
    }
  }
}
