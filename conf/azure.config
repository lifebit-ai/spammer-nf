params {
    queue = 'auto'
}

process {
    queue = params.queue
    withName: processA {
      queue = params.queue
    }
    withName: processB {
      queue = params.queue
    }
    withName: processC {
      queue = params.queue
    }
    withName: processD {
      queue = params.queue
    }
}
